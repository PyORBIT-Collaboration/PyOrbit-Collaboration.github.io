
.. _program_listing_file__home_runner_work_PyORBIT3_PyORBIT3_src_orbit_BunchDiagnostics_BunchTwissAnalysis.hh:

Program Listing for File BunchTwissAnalysis.hh
==============================================

|exhale_lsh| :ref:`Return to documentation for file <file__home_runner_work_PyORBIT3_PyORBIT3_src_orbit_BunchDiagnostics_BunchTwissAnalysis.hh>` (``/home/runner/work/PyORBIT3/PyORBIT3/src/orbit/BunchDiagnostics/BunchTwissAnalysis.hh``)

.. |exhale_lsh| unicode:: U+021B0 .. UPWARDS ARROW WITH TIP LEFTWARDS

.. code-block:: cpp

   #ifndef BUNCH_TWISS_ANALYSIS_H
   #define BUNCH_TWISS_ANALYSIS_H
   
   //pyORBIT utils
   #include "CppPyWrapper.hh"
   
   #include "Bunch.hh"
   
   using namespace std;
   
   
   class BunchTwissAnalysis: public OrbitUtils::CppPyWrapper
   {
       public:
   
           BunchTwissAnalysis();
   
           virtual ~BunchTwissAnalysis();
   
           void analyzeBunch(Bunch* bunch);
   
           double getCorrelation(int ic, int jc);
   
           double getAverage(int ic);
   
           int getGlobalCount();
   
           double getGlobalMacrosize();
   
           double getEffectiveEmittance(int ic);
   
           double getEffectiveAlpha(int ic);
   
           double getEffectiveBeta(int ic);
   
           double getEffectiveGamma(int ic);
   
           double getEmittance(int ic);
   
           double getEmittanceNormalized(int ic);
   
           double getAlpha(int ic);
   
           double getBeta(int ic);
   
           double getGamma(int ic);
   
           double getDispersion(int ic);
   
           double getDispersionDerivative(int ic);
   
           void computeBunchMoments(Bunch* bunch, int order, int dispersionflag, int emitnormflag);
   
           double getBunchMoment(int i, int j);
   
       private:
   
           int count;
   
           double total_macrosize;
   
           double* avg_arr;
           double* avg_arr_MPI;
   
           double* corr_arr;
           double* corr_arr_MPI;
   
           double** momentXY;
   
           double _order;
   
           double bunch_momentum;
           double bunch_gamma;
           double bunch_beta;
           double bunch_kinenergy;
           double bunch_mass;
   
   };
   
   #endif
   //endif for BUNCH_TWISS_ANALYSIS_H
